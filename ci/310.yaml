name: test
channels:
  - conda-forge
dependencies:
  - python=3.10
  # required
  - git
  - libpysal
  - numpy
  - pip
  - scipy
  - spreg
  # testing
  - codecov
  - pytest
  - pytest-cov
  - pytest-xdist
